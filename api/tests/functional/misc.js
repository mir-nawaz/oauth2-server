'use strict';

const expect = require('chai').expect;

describe('misc', function() {
  this.timeout(10000);

  before(function() {});

  afterEach(function() {});

  after(function() {});

  it('test', function() {
    expect(true).to.equal(true);
  });

});
